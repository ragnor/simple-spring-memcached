<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>net.nelz.simplesm</groupId>
		<artifactId>simple-spring-memcached-parent</artifactId>
		<version>2.0.0-SNAPSHOT</version>
	</parent>
	<artifactId>integration-test</artifactId>
	<packaging>jar</packaging>
	<name>integration-test</name>
	<url>http://code.google.com/p/simple-spring-memcached/</url>
	<dependencies>
		<dependency>
			<groupId>net.nelz.simplesm</groupId>
			<artifactId>simple-spring-memcached</artifactId>
			<version>${project.parent.version}</version>
			<type>jar</type>
		</dependency>
		<dependency>
			<groupId>commons-lang</groupId>
			<artifactId>commons-lang</artifactId>
			<version>2.6</version>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-context</artifactId>
			<version>${spring.version}</version>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-test</artifactId>
			<version>${spring.version}</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<profiles>
		<profile>
			<id>spymemcached</id>
			<build>
				<testResources>
					<testResource>
						<directory>${basedir}/src/test/resources/spymemcached</directory>
					</testResource>
				</testResources>
			</build>
			<dependencies>
				<dependency>
					<groupId>spy</groupId>
					<artifactId>memcached</artifactId>
					<version>${spymemcached.version}</version>
				</dependency>
			</dependencies>
		</profile>

		<profile>
			<id>xmemcached</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<build>
				<testResources>
					<testResource>
						<directory>${basedir}/src/test/resources/xmemcached</directory>
					</testResource>
				</testResources>
			</build>
			<dependencies>
				<dependency>
					<groupId>com.googlecode.xmemcached</groupId>
					<artifactId>xmemcached</artifactId>
					<version>${xmemcached.version}</version>
				</dependency>
			</dependencies>
		</profile>
	</profiles>

</project>
